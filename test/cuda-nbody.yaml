apiVersion: v1
kind: Pod
metadata:
  name: cuda-nbody
spec:
  restartPolicy: Never
  containers:
    - name: nbody
      image: nvcr.io/nvidia/k8s/cuda-sample:nbody
      args: ["-benchmark", "-numbodies=512000"]
      resources:
        limits:
          nvidia.com/gpu: 1
