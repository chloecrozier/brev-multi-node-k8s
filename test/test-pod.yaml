apiVersion: v1
kind: Pod
metadata:
  name: sandbox-test
  labels:
    role: sandbox
spec:
  containers:
    - name: jupyter
      image: jupyter/scipy-notebook:latest
      ports:
        - containerPort: 8888
      resources:
        requests:
          cpu: "250m"
          memory: "256Mi"